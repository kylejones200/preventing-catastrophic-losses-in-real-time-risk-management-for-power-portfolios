[package]
name = "preventing_catastrophic_losses_in_real_time_risk_management_for_power_portfolios_bench"
description = "Standalone Rust benchmark (no Python)"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "bench"
path = "src/main.rs"

[dependencies]
preventing_catastrophic_losses_in_real_time_risk_management_for_power_portfolios_core = { path = "../core" }
